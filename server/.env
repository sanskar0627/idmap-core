# ========================================
# ⚙️ DKG / SIGN SERVER CONFIGURATION
# ========================================

# Number of participants
N=2

# Unique numeric ID for this server node
# This ID differentiates each participant in the distributed key generation process.
NODE_ID=0

# ========================================
# 🔌 REDIS CONFIGURATION
# ========================================

# Redis connection URL
# Used for Pub/Sub communication between nodes (e.g., dkg-start, sign-start, etc.)
REDIS_URL=redis://127.0.0.1:6379

# ========================================
# 🌐 TCP NETWORK CONFIGURATION
# ========================================

# The TCP listener address for the DKG server
# This server listens for incoming connections from clients during the DKG phase.
DKG_SERVER_ADDR=0.0.0.0:7001

# The TCP listener address for the Signing server
# This server listens for incoming connections from clients during the signing phase.
SIGN_SERVER_ADDR=0.0.0.0:7002

# ========================================
# 🧠 SESSION DEFAULTS
# ========================================

# Default session identifier, used when a session ID is not provided dynamically.
DEFAULT_SESSION_ID=session-001

# ========================================
# 🪣 DATABASE CONFIGURATION (OPTIONAL)
# ========================================

# Connection string for PostgreSQL (if you plan to store shares or metadata)
# Example format: postgres://<username>:<password>@<host>:<port>/<database>
# POSTGRES_URL=postgres://postgres:password@localhost:5432/idmap

# ========================================
# 🔐 SECURITY & ENTROPY (OPTIONAL)
# ========================================

# Optional: Path to an additional entropy source for randomness during key generation
# ENTROPY_SOURCE=/dev/urandom

# ========================================
# 🧾 LOGGING & DEBUG (OPTIONAL)
# ========================================

# Log level can be: error, warn, info, debug, or trace
# LOG_LEVEL=info

# Enable verbose output for debugging Redis or TCP interactions
# DEBUG=true
